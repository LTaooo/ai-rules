# 全局规则

完整全局规则维护在 `~/ai-rule/AGENTS.md`（单点维护源，本文件只是入口）。

`~` 表示当前运行环境的用户主目录，不是项目目录。工具不支持 `~` 展开时，先获取用户主目录再拼接 `ai-rule/AGENTS.md`；PowerShell 中使用 `$HOME`。

会话开始后，先完整读取 `~/ai-rule/AGENTS.md`，把其中全部规则当作全局约定严格遵循；本文件自身不保存具体规则。
